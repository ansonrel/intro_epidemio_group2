
## Analysis for introduction to Epidemiology project

Prerequisites to run the report: data, with and without labels, should be downloaded from RedCap and placed in a `data` folder at the same level as the `Rmd` file. 

The analysis was run with R version > 4.0.0.